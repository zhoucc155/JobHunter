# 单段真人风 · 打招呼提示词草稿（待确认，未落地）

> 状态：**仅草稿，未改动任何代码**。替换目标 = `extension/background.js` 的 `genGreeting`（行 108–155 区域）。
> 这是对你 16:16 给的「周彩妮」例子的形态化——单段口语、自然关联 JD、坦诚学习意愿、硬量化数据。
> ⚠️ 与 16:04 的「四段式压缩方案」冲突：本草稿是**形态变更**（四段→单段），故四段压缩方案作废。

---

## 1. 新 `sys` 数组（直接替换行 114–130）

```js
const sys = [
  '你是候选人本人，在BOSS直聘上给招聘方写首次打招呼消息。像真人加微信后的第一句话——自然、真诚、不套路，不是培训稿也不是自我介绍背诵。',
  '',
  '【字数】单段、不分段、不含换行，目标 120~160 字（上限严格 160，超出即不合格；能讲清楚就不必凑字数，短于 110 字也可接受）。',
  '',
  '【写作结构 · 单段自然语流，不要分点、不要编号、不要「第一第二」】',
  '1) 以「您好，我是{简历里的真实姓名}」开头，紧接着一句话说清：几年 + 什么核心领域经验（如「我有7年社区和创作者运营经验」）。',
  `2) 自然带出你关注到这家公司在做什么 / 这个岗位为何吸引你（如「小红书是UGC社区标杆，我一直在关注其种草经济生态」「看到贵司正在拓展新品类」）。只基于岗位JD里能看出的方向，不编造公司未公开的信息。${nameRule}`,
  '3) 讲 1 个与岗位最匹配的项目经历：做了什么、解决了什么业务问题、用了什么方法、拿到什么量化结果。数据必须来自简历，不得虚构。',
  '4) （可选）若确有跨领域或能力缺口，坦诚说明学习意愿与可迁移能力（如「虽然对AIGC还在深入学习，但我的用户洞察能力能快速迁移」）；若经验完全匹配则不必刻意谦虚，不要每封都套这句。',
  '5) 以期待进一步交流收尾（如「期待为平台挖掘优质创作者」「期待交流」）。',
  '',
  '【真实性铁律 · 违反即不合格】',
  '· 只能使用简历里明确写过的事实（公司/项目/工具/技能/数字/年限）；',
  '· 严禁编造简历没有的量化数据、项目或公司动向；',
  '· 「贵司正在…」只能基于JD已写明的内容推断，不得脑补未公开战略；',
  '· 姓名必须用简历真实姓名，不得写「xxx」占位或示例姓名。',
  '',
  '【风格】纯文本输出，不要markdown、不要引号包裹、不要落款；像候选人与HR的首次真实沟通，专业但自然；禁止「非常符合」「高度匹配」等AI腔；禁止罗列所有经历。'
].join('\n');
```

## 2. 需同步改 `nameRule` 措辞（行 111–113）

现 `nameRule` 写「第一段必须以…开头」，新结构无「段」概念。把两处「第一段」改为「开头」即可，逻辑（有姓名 / 从简历提取 / 您好！）保留：

```js
const nameRule = userName
  ? `开头必须以「您好，我是${userName}，」开头，紧接职业定位与对岗位的关注。`
  : '开头必须以「您好，我是XX，」开头（XX=从简历中提取的候选人真实姓名；若简历中确实没有姓名，则以「您好！」开头），紧接职业定位。绝不允许编造姓名或输出"您好，我是xxx"这类占位符。';
```

## 3. Few-shot 构造（放进 `messages`，行 134–137 区域）

把你的例子当**语气参考**，但严格隔离其具体数据，避免模型把"周彩妮/52万+/7年"安到用户头上：

```js
const fewShotExample =
  '您好，我是周彩妮，我对AIGC创作者运营岗位很感兴趣。我有7年社区和创作者运营经验，' +
  '曾主导"声音鉴定"创新玩法，上线3天UV达52万+，带动直播DAU提升30%。' +
  '我擅长通过活动策划和社群运营提升创作者活跃和内容产出，虽然目前对AIGC领域还在深入学习，' +
  '但我相信我的用户洞察和活动落地能力能快速迁移，期待为平台挖掘和孵化优质AIGC创作者。';

const messages = [
  { role: 'system', content: sys },
  { role: 'user', content:
    `【目标岗位】${job.title}｜${job.company}${kwLine}${nameLine}\n` +
    `【岗位JD】\n${(job.jd || '').slice(0, 2000)}\n\n` +
    `【我的完整简历】\n${resumeText.slice(0, 2500)}\n\n` +
    `——下面这条仅作语气/句式参考，你必须用上面简历里的真实信息生成，` +
    `严禁使用示例中的姓名"周彩妮"和她的任何数据：\n${fewShotExample}` }
];
```

## 4. 压缩 / 兜底提示改写（行 140–154）

把「四段结构 / 段落换行」去掉，改为「单段不分段 / 保开头自称 / 保案例数据」：

```js
// 超长时不硬剪：让 AI 自己压缩重写（保单段、保完整收尾），最多2轮
for (let round = 0; round < 2 && plainLen(text) > 162; round++) {
  const over = plainLen(text) - 160;
  const compressMsgs = [
    ...messages,
    { role: 'assistant', content: text },
    { role: 'user', content:
      `上面这版共${plainLen(text)}字（不含换行），超出160字上限约${over}字。` +
      `请压缩重写：保持单段不分段、以「您好，我是XX」开头、案例核心数据保留，` +
      `优先删减修饰词和次要细节，结尾必须是完整句子。直接输出压缩后的全文，不要任何解释。` }
  ];
  text = (await callDeepSeek(compressMsgs, apiKey, false)).trim();
}

// 最终兜底：仍超长则在句子边界优雅截断
if (plainLen(text) > 160) {
  text = truncateAtSentence(text, 158);
}
```

## 5. 保留不动

- 行 108 注释改为「单段真人风」即可（非代码逻辑）。
- `plainLen` / `truncateAtSentence` 函数体（行 88–97）不动。
- 调用点（GEN_GREETING 处）不动，`genGreeting(job, resumeText, apiKey, userName)` 签名不变。

## 6. 落地前仍需你拍板（见 16:16 对话）

- **A 替换 vs 开关**：强烈建议做成「格式开关」新选项（四段式 / 单段真人风），保留可回退 + A/B 对比。
- **B 简历数据**：例子能成立靠硬量化数据（UV/DAU/留存%）。若简历无对应量化，模型易空洞或编造——这是格式能否"真"的硬前提。
- **C 防幻觉**：本草稿已在提示词内写死「真实性铁律」，等价于路线 A 精神；是否再叠加大模型的 LLM 检测/重写（路线 A 函数）待定。
