# 打招呼文案压缩方案（目标 130–160 字）

> 状态：方案待确认，**尚未改动任何代码**。当前 `extension/` 为 pre-A 纯四段式原始形态。

## 目标
将 `genGreeting` 生成的四段式招呼语从「硬上限 195 / 目标 165~190」压缩到 **130~160 字**。

## 设计原则（三档自洽）
- **目标软区间 130~160**：靠生成提示词约束，AI 尽量落在区间内。
- **压缩触发 162**：生成 >162 字进压缩循环（最多 2 轮），让 AI 智能压到 ≤160，保留四段结构与案例核心。
- **硬兜底 160**：若压缩后仍 >160，按句子边界截断到 158（砍 2 字，绝不砍断第四段收尾）。

161–162 字轻微超 → 直接兜底句子边界砍 2–4 字（安全，不伤案例）；163+ → 进压缩循环。

## 改动清单（仅 `extension/background.js`，单文件）

| 行号 | 位置 | 旧值 | 新值 |
|---|---|---|---|
| 108 | 函数注释 | `200字内` | `160字内` |
| 117 | 生成提示词·字数硬约束 | `全文不含换行严格不超过195字（目标区间165~190字）` | `全文不含换行严格不超过160字（目标区间130~160字）` |
| 129 | 生成提示词·输出前自检 | `若超过195字，先自行删减` | `若超过160字，先自行删减` |
| 141 | 压缩循环触发阈值 | `plainLen(text) > 200` | `plainLen(text) > 162` |
| 142 | over 计算 | `plainLen(text) - 195` | `plainLen(text) - 160` |
| 146 | 压缩提示文案 | `超出195字上限约${over}字` | `超出160字上限约${over}字` |
| 152 | 兜底触发阈值 | `plainLen(text) > 200` | `plainLen(text) > 160` |
| 153 | 兜底截断 | `truncateAtSentence(text, 198)` | `truncateAtSentence(text, 158)` |

**保留不动**：行 117 的「第二段最多保留2个最亮眼数据、第三段/第四段各控制在30字以内」（本就是压字数约束，正好服务新目标）；`plainLen` / `truncateAtSentence` 函数体；所有调用点。

## 可行性 & 风险
- 技术零风险：纯数字/文案替换，无新函数、无新调用、不碰其它文件。
- 文案质量：四段式 195→160（减 ~35 字），主要靠砍修饰词与第二段次要细节，案例四要素可保留；第二段信息密度略降是压缩必然代价。
- 下限 130：提示词软约束即可（四段式天然 >130），不做硬卡——硬卡短文本需"补写"，反而画蛇添足。

## 验证（落地后）
1. `node --check extension/background.js` 语法通过。
2. `vm` 加载真实 background.js + stub `callDeepSeek`：
   - 返回 170 字 → 触发压缩/兜底，终态 ≤160；
   - 返回 158 字 → 原样返回（不误伤）；
   - 返回 162 字 → 兜底砍到 ≤158 且不在句子中间断。

## 落地步骤（待确认）
1. 建带时间戳备份 `extension-backup-YYYYMMDD-HHMM-compact`。
2. 按上表 8 处 Edit 改 `background.js`。
3. 跑语法 + 单测。
4. `chrome://extensions` 重载扩展，生成 3~5 条确认落在 130~160 且四段完整。
